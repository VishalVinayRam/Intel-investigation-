# Trivy Ignore File
# Add CVE IDs (e.g., CVE-2023-12345) or GHSA IDs here to explicitly ignore false positives
# or accepted risks during the SCA scan.

# Explicitly ignore remaining HIGH vulnerabilities with 'fix_deferred', 'affected', or 'will_not_fix' 
# status in Debian base images to allow the CI pipeline to pass while upstream vendors
# develop or backport security patches.

# glibc: iconv() DoS (fix_deferred in Debian Bookworm/Trixie)
CVE-2026-4046

# ncurses: Buffer overflow (affected, awaiting patch in stable / non-interactive container)
CVE-2025-69720

# systemd: Spurious IPC ACE/DoS (affected, no systemd daemon in container)
CVE-2026-29111

# zlib1g: integer overflow in zipOpenNewFileInZip4_6 (will_not_fix in Bookworm)
CVE-2023-45853
